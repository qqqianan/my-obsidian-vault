具体对比修正可以分成一句话：  **把“实时已保存的事件列表”和“补偿重算出来的正确事件列表”都标准化成同一种事件模型，然后按时间重叠匹配，匹配上就更新，匹配不上就新增或撤销。**  你们现在不能只靠 `uuid` 对比。因为当前 `uuid` 是这样生成的：  ```java terminalId + "-" + gpsTimeStamp + "-" + alarmType ```  也就是事件开始点时间参与了 `uuid`。补传后如果开始时间被修正，`uuid` 会变，所以对比时必须按 **terminalId + alarmType + alarmStatus + 时间区间重叠** 来匹配。  **标准事件模型** 先把两边都转成这个结构：  ```java class EventSegment {     Long terminalId;     Integer alarmType;     String alarmStatus;     Long startTime;     Long endTime;     String eventId;     Point startPoint;     Point endPoint;     String source; // REALTIME / COMPENSATE } ```  实时已入库的数据来自 `car_event_alarm`。  补偿重算结果来自完整 0200 点序列，例如：  ```text 按 terminalId + gpsTimeStamp 排序后重新跑规则 ```  **第一步：取受影响旧事件** 比如迟到点是 `10:20`，补偿核心区间是：  ```text 10:10 ~ 10:40 ```  查询旧事件不能只查 `start_time between 10:10 and 10:40`，要查所有和这个区间有交集的事件：  ```sql where terminal_id = ?   and alarm_type in (...)   and (     start_time <= :rangeEnd     and coalesce(end_time, now()) >= :rangeStart   ) ```  否则一个 `10:00 ~ 10:50` 的旧事件会漏掉。  **第二步：重算正确事件** 从原始 0200 表查更宽一点的点：  ```text 09:50 ~ 11:00 ```  这个叫 warm-up / look-ahead。因为事件起止依赖前后点，不能只拿迟到点附近一小段。  然后按 GPS 时间排序，得到补偿结果：  ```text newEvents = ruleEngine.calculate(points) ```  最后只保留和核心区间有交集的事件参与对比。  **第三步：匹配旧事件和新事件** 匹配优先级：  ```text terminalId 相同 alarmType 相同 alarmStatus 相同 时间区间有重叠 重叠比例最高 ```  重叠计算：  ```text overlap = min(old.end, new.end) - max(old.start, new.start) ```  如果 `overlap > 0`，说明两个事件描述的是同一段业务事实。  可以加一个容忍阈值，比如：  ```text 开始/结束偏差 <= 60 秒 或者 overlap / min(oldDuration, newDuration) >= 0.5 ```  **第四步：生成修正动作** 对比后只有四类动作。  ```text NOOP：旧事件和新事件一致，不处理 UPDATE：匹配上了，但开始/结束/位置/里程等字段不同，更新旧事件 INSERT：新事件没有匹配到旧事件，新增 CANCEL：旧事件没有匹配到新事件，撤销旧事件 ```  例子：  ```text 旧事件：怠速 10:00 ~ 10:20 新事件：怠速 10:00 ~ 10:50 => UPDATE end_time = 10:50 ```  ```text 旧事件：无 新事件：超速 10:12 ~ 10:18 => INSERT ```  ```text 旧事件：换油 10:05 ~ 10:08 新事件：无 => CANCEL ```  **第五步：怎么落库** 你们现在 `car_event_alarm` 没有 `is_deleted`、`source`、`revision` 这类字段，只有 `terminal_id + event_id` 和开始/结束字段。所以我建议补偿不要继续走现在的 `batchSaveAlarm` 追加逻辑，而是新增一个补偿保存方法：  ```java applyCompensation(List<EventCorrection> corrections) ```  其中：  ```java class EventCorrection {     String op; // INSERT / UPDATE / CANCEL     String oldEventId;     EventSegment newEvent;     String reason; // LATE_0200_RECOMPUTE } ```  落库策略：  ```text INSERT：插入新事件 UPDATE：按 oldEventId + terminalId 更新 start/end/点位/里程/状态；必要时同步更新 event_id CANCEL：如果允许物理删除就 delete；更推荐加 is_deleted/correction_status 标记撤销 ```  我更推荐给表加字段：  ```text correction_status: NORMAL / CORRECTED / CANCELED correction_source: REALTIME / COMPENSATE correction_time revision ```  这样历史可追溯，不会出现“补偿任务把事件删了但查不到原因”的问题。  **最关键的一点** 对比时不要用 `uuid` 当唯一判断依据，只能用它定位旧记录。真正判断“是不是同一个事件”，要靠：  ```text terminalId + alarmType + alarmStatus + 时间区间重叠 ```  然后再决定更新、插入、撤销。当前代码的 `uuid` 绑定了开始 GPS 时间，一旦补传改变开始点，直接按 `uuid` 对比一定会变成“旧的撤销 + 新的新增”，这在业务上可能可以接受，但展示上会比较跳。更好的做法是匹配到同一事件后更新原记录。# ai出的实时计算补传数据补偿算法

- Date: 2026-06-25 19:15
- Type: daily-communication
- Domain: unknown
- Status: inbox
- People:
- Source: 日常沟通

## 原始记录

具体对比修正可以分成一句话：  **把“实时已保存的事件列表”和“补偿重算出来的正确事件列表”都标准化成同一种事件模型，然后按时间重叠匹配，匹配上就更新，匹配不上就新增或撤销。**  你们现在不能只靠 `uuid` 对比。因为当前 `uuid` 是这样生成的：  ```java terminalId + "-" + gpsTimeStamp + "-" + alarmType ```  也就是事件开始点时间参与了 `uuid`。补传后如果开始时间被修正，`uuid` 会变，所以对比时必须按 **terminalId + alarmType + alarmStatus + 时间区间重叠** 来匹配。  **标准事件模型** 先把两边都转成这个结构：  ```java class EventSegment {     Long terminalId;     Integer alarmType;     String alarmStatus;     Long startTime;     Long endTime;     String eventId;     Point startPoint;     Point endPoint;     String source; // REALTIME / COMPENSATE } ```  实时已入库的数据来自 `car_event_alarm`。  补偿重算结果来自完整 0200 点序列，例如：  ```text 按 terminalId + gpsTimeStamp 排序后重新跑规则 ```  **第一步：取受影响旧事件** 比如迟到点是 `10:20`，补偿核心区间是：  ```text 10:10 ~ 10:40 ```  查询旧事件不能只查 `start_time between 10:10 and 10:40`，要查所有和这个区间有交集的事件：  ```sql where terminal_id = ?   and alarm_type in (...)   and (     start_time <= :rangeEnd     and coalesce(end_time, now()) >= :rangeStart   ) ```  否则一个 `10:00 ~ 10:50` 的旧事件会漏掉。  **第二步：重算正确事件** 从原始 0200 表查更宽一点的点：  ```text 09:50 ~ 11:00 ```  这个叫 warm-up / look-ahead。因为事件起止依赖前后点，不能只拿迟到点附近一小段。  然后按 GPS 时间排序，得到补偿结果：  ```text newEvents = ruleEngine.calculate(points) ```  最后只保留和核心区间有交集的事件参与对比。  **第三步：匹配旧事件和新事件** 匹配优先级：  ```text terminalId 相同 alarmType 相同 alarmStatus 相同 时间区间有重叠 重叠比例最高 ```  重叠计算：  ```text overlap = min(old.end, new.end) - max(old.start, new.start) ```  如果 `overlap > 0`，说明两个事件描述的是同一段业务事实。  可以加一个容忍阈值，比如：  ```text 开始/结束偏差 <= 60 秒 或者 overlap / min(oldDuration, newDuration) >= 0.5 ```  **第四步：生成修正动作** 对比后只有四类动作。  ```text NOOP：旧事件和新事件一致，不处理 UPDATE：匹配上了，但开始/结束/位置/里程等字段不同，更新旧事件 INSERT：新事件没有匹配到旧事件，新增 CANCEL：旧事件没有匹配到新事件，撤销旧事件 ```  例子：  ```text 旧事件：怠速 10:00 ~ 10:20 新事件：怠速 10:00 ~ 10:50 => UPDATE end_time = 10:50 ```  ```text 旧事件：无 新事件：超速 10:12 ~ 10:18 => INSERT ```  ```text 旧事件：换油 10:05 ~ 10:08 新事件：无 => CANCEL ```  **第五步：怎么落库** 你们现在 `car_event_alarm` 没有 `is_deleted`、`source`、`revision` 这类字段，只有 `terminal_id + event_id` 和开始/结束字段。所以我建议补偿不要继续走现在的 `batchSaveAlarm` 追加逻辑，而是新增一个补偿保存方法：  ```java applyCompensation(List<EventCorrection> corrections) ```  其中：  ```java class EventCorrection {     String op; // INSERT / UPDATE / CANCEL     String oldEventId;     EventSegment newEvent;     String reason; // LATE_0200_RECOMPUTE } ```  落库策略：  ```text INSERT：插入新事件 UPDATE：按 oldEventId + terminalId 更新 start/end/点位/里程/状态；必要时同步更新 event_id CANCEL：如果允许物理删除就 delete；更推荐加 is_deleted/correction_status 标记撤销 ```  我更推荐给表加字段：  ```text correction_status: NORMAL / CORRECTED / CANCELED correction_source: REALTIME / COMPENSATE correction_time revision ```  这样历史可追溯，不会出现“补偿任务把事件删了但查不到原因”的问题。  **最关键的一点** 对比时不要用 `uuid` 当唯一判断依据，只能用它定位旧记录。真正判断“是不是同一个事件”，要靠：  ```text terminalId + alarmType + alarmStatus + 时间区间重叠 ```  然后再决定更新、插入、撤销。当前代码的 `uuid` 绑定了开始 GPS 时间，一旦补传改变开始点，直接按 `uuid` 对比一定会变成“旧的撤销 + 新的新增”，这在业务上可能可以接受，但展示上会比较跳。更好的做法是匹配到同一事件后更新原记录。

## 初步结论

-

## 待确认

-

## 可能关联

-

